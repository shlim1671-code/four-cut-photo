# 4컷 사진 웹앱 — 1단계 MVP 스펙

> Claude Code 작업 지시서. 이 문서 전체를 Claude Code에 붙여넣고 "이 스펙대로 1단계를 구현해줘"라고 요청하면 된다.

## 1. 프로젝트 개요

사용자가 사진을 넣고 → 보정하고 → 프레임을 골라 → 다운로드하는 순수 프론트엔드 웹앱.
백엔드/DB 없음. 모든 처리는 브라우저 Canvas에서 일어난다. 정적 호스팅(GitHub Pages, Vercel)에 배포.

사용자 흐름:

```
사진 넣기(업로드 또는 웹캠 촬영) → 칸 배치/조정 → 보정 → 프레임 선택 → PNG 다운로드
```

프레임은 개발자가 코드 내 JSON 정의로 관리한다. 사용자용 프레임 에디터는 1단계 범위 밖.

## 2. 기술 스택

- React 18 + Vite + TypeScript
- Tailwind CSS
- 이미지 처리: HTML5 Canvas API (라이브러리 의존 최소화)
- 상태관리: React 내장 (useState/useReducer/Context). 외부 상태 라이브러리 불필요
- 배포 산출물: 정적 파일. `vite build` 결과를 그대로 호스팅

## 3. 폴더 구조

```
src/
  components/
    PhotoInput/        업로드 + 웹캠 촬영
    SlotEditor/        칸별 사진 배치(줌·위치·반전·회전)
    AdjustPanel/       보정 슬라이더 + 필터 프리셋
    FramePicker/       프레임 선택 UI
    CanvasPreview/     실시간 합성 미리보기
    ExportButton/      최종 PNG 생성·다운로드
  frames/
    definitions.ts     프레임 JSON 정의 모음 (개발자가 여기에 추가)
    types.ts           프레임 타입 정의
  lib/
    imageProcessing.ts Canvas 보정 로직 (밝기/대비/채도 등)
    filters.ts         필터 프리셋 정의
    compositor.ts      최종 합성 로직
  state/
    editorContext.tsx  앱 전역 상태
  App.tsx
  main.tsx
```

## 4. 프레임 정의 구조

`src/frames/definitions.ts`에 아래 형태로 프레임을 추가한다. 칸 위치는 프레임 대비 **비율(0~1)**로 지정 — 어떤 출력 해상도에서도 동일하게 동작.

```typescript
interface FrameSlot {
  x: number; y: number; w: number; h: number;  // 0~1 비율
}

interface FrameDecoration {
  type: 'text' | 'image';
  content: string;            // text면 문구, image면 에셋 경로
  x: number; y: number;       // 0~1 비율, 중심점 기준
  font?: string;
  fontSize?: number;          // 프레임 폭 대비 비율
  color?: string;
  align?: 'left' | 'center' | 'right';
}

interface FrameDefinition {
  id: string;
  name: string;
  aspectRatio: number;        // width / height
  background: string;         // 색상 또는 배경 이미지 경로
  borderRadius?: number;
  slots: FrameSlot[];         // 4개 (4컷)
  decorations?: FrameDecoration[];
}
```

1단계 프리셋으로 최소 4종 제공: 세로 인생네컷 스타일, 2×2 그리드, 무지 흰 프레임, 어두운 행사용 프레임.

새 프레임 추가는 이 배열에 객체 하나 push하면 끝.

## 5. 기능 명세

### 5.1 사진 입력 (PhotoInput)
- 파일 업로드: 드래그앤드롭 + 파일선택. 여러 장 한 번에 가능
- 웹캠 촬영: `getUserMedia` API. 촬영 버튼, 카운트다운(3·2·1) 옵션
- 입력된 사진은 칸 슬롯에 순서대로 자동 배치, 이후 수동 재배치 가능

### 5.2 칸 배치/조정 (SlotEditor)
칸별로 독립 적용:
- 사진 위치 이동 (드래그/터치)
- 줌 인/아웃 (핀치 또는 슬라이더)
- 좌우반전 토글
- 회전 (90도 단위 + 미세 회전)
- 칸 비우기 / 다른 사진으로 교체

### 섹션 5.3 (개정) — 보정: 프리셋 + 토글
1단계의 슬라이더 중심 보정 UI를 사용자 화면에서 제거한다.
사용자는 프리셋을 고르고, 피부 보정을 켜고 끄기만 한다.
사용자가 보는 것 (단계 4)

필터 프리셋 선택: 원본 / 부스톤 / 필름 흑백 / 빈티지 컬러 / 쿨톤
(썸네일 또는 버튼으로 선택. 추후 프리셋 추가 가능)
피부 보정 토글: on / off 하나. 강도 슬라이더 없음.
on일 때의 강도는 자연스러운 고정값으로 미리 정해둠.

코드에 유지하는 것 (사용자에게 안 보임)

imageProcessing.ts의 슬라이더 연산(밝기·대비·채도·색온도·하이라이트·
섀도우·선명도·그레인)은 삭제하지 않는다
프리셋은 이 연산들의 값 조합으로 정의된다 (filters.ts)
즉 슬라이더는 "프리셋을 만들고 튜닝하기 위한 내부 재료"로 남는다
운영자(개발자)는 filters.ts 수치를 조정해 프리셋을 만들고 다듬는다

의도

사용자 화면은 가볍게: 고르는 것만, 만지는 슬라이더 없음
보정 품질은 운영자가 프리셋을 잘 세팅해 책임진다
시중 부스 경험과 동일: 사용자는 "선택", 보정은 "미리 세팅됨"

### 5.4 프레임 선택 (FramePicker)
- definitions.ts의 프레임을 썸네일 그리드로 표시
- 선택 즉시 미리보기 반영
- 배경색만 빠르게 바꾸는 간단 옵션 (선택)

### 5.5 미리보기 (CanvasPreview)
- 프레임 + 4컷 사진 + 보정 + 데코레이션을 합성한 실시간 미리보기
- 성능을 위해 미리보기는 화면 표시 해상도로만 렌더

### 5.6 내보내기 (ExportButton)
- 최종 합성을 원본 해상도(상한 적용)로 렌더 후 PNG 다운로드
- JPG 옵션 + 품질 슬라이더 (선택)
- 다운로드 파일명에 날짜 자동 포함

## 6. 성능·기술 주의사항

- **출력 해상도 상한**을 둔다 (예: 긴 변 2400px). 4장 고해상도 합성은 모바일에서 메모리 초과 위험
- 미리보기와 최종 export의 해상도를 분리: 미리보기는 가볍게, export는 한 번만 고품질
- 픽셀 단위 보정은 무거우므로, 슬라이더 조작 중에는 다운스케일 이미지에 적용 (debounce)
- 웹캠은 HTTPS에서만 동작 → 배포 호스팅이 HTTPS인지 확인 (GitHub Pages·Vercel 기본 제공)
- 이미지 EXIF 회전 처리: 모바일 사진은 방향 메타데이터가 있으므로 로드 시 정방향 보정
- 모든 이미지 처리는 클라이언트에서만 — 사진이 서버로 전송되지 않음 (개인정보 안전, 행사용 장점)

## 섹션 7 (개정) — 화면 흐름: 4단계 마법사
앱은 한 화면에 모든 도구를 펼치지 않는다. 시중 네컷 스튜디오처럼
순서가 정해진 4단계를 따라간다. 사용자는 한 번에 한 단계만 보며,
부스를 처음 쓰는 사람도 그냥 따라가면 된다.
단계 1 — 프레임 고르기

definitions.ts의 프레임을 썸네일로 보여주고 하나 선택
선택하면 다음 단계로

단계 2 — 사진 확보

입력 방식을 사용자가 고른다: [지금 찍기] 또는 [불러오기]
[지금 찍기]: 웹캠 + 타이머 촬영. 여러 장(8~12장 권장)을 연속 촬영
[불러오기]: 기기에서 사진 여러 장 선택 업로드
어느 방식이든 결과는 동일하게 "사진 여러 장의 모음"
일정 장수 이상 모이면 다음 단계로

단계 3 — 4장 고르기

단계 2에서 모은 사진들을 그리드로 보여줌
사용자가 프레임의 칸 수(4장)만큼 선택
선택 순서가 곧 칸 배치 순서 (1번째 선택 → 칸1 ...)
선택 취소·재선택으로 순서 조정 가능
4장이 채워지면 다음 단계로

단계 4 — 마무리

프레임 + 고른 4장이 합성된 결과를 크게 보여줌
프리셋 선택 (아래 5.3 개정 참조)
피부 보정 on/off 토글
칸별 미세 조정(위치·줌·반전·회전)은 이 단계에서 가능
완료되면 다운로드 (ExportButton)

단계 이동 규칙

단계 사이를 앞뒤로 이동 가능 (이전 단계로 돌아가 다시 고르기)
현재 어느 단계인지 항상 표시 (1/4, 2/4 ...)
행사용: 한 사람이 끝나면 처음(단계 1)으로 깔끔히 초기화되는 경로 필요

레이아웃 (디자인 섹션 10 유지)

미니멀·화이트, 흑백회색, 터치 우선, 탭 요소 44px 이상은 그대로
각 단계 화면은 단순하게: 그 단계에 필요한 것만 보여줌
폰 세로·태블릿 가로 모두 같은 4단계 흐름

## 8. 1단계 완료 기준 (Definition of Done)

- [ ] 파일 업로드와 웹캠 촬영 둘 다 동작
- [ ] 4개 칸에 사진 배치, 칸별 줌·이동·반전·회전 동작
- [ ] 보정 슬라이더 7종 + 필터 프리셋 6종 동작, 실시간 미리보기
- [ ] 프리셋 프레임 최소 4종, 선택 시 즉시 반영
- [ ] PNG 다운로드, 출력 해상도 상한 적용
- [ ] 데스크톱·모바일 반응형
- [ ] `vite build`로 정적 배포 산출물 생성 확인

## 9. 다음 단계 (1단계 범위 밖, 참고용)

- 2단계: 프레임 색·여백·로고·텍스트 사용자 커스터마이징, 로컬 저장
- 3단계: 프레임 정의 JSON 내보내기/불러오기, URL 공유
- 4단계: 공개 갤러리 (이때부터 Supabase 등 백엔드 필요)

각 단계는 1단계 완성 후 별도로 Claude Code에 지시할 것. 한 번에 전부 만들지 말 것.

## 10. 디자인 시스템

> SPEC.md 섹션 7(UI 레이아웃)을 이 섹션이 보강한다. 모든 컴포넌트는
> 이 디자인 시스템을 따라 만든다. 방향: **미니멀·화이트**. 사진과
> 프레임이 주인공이며 UI는 배경으로 물러난다.

### 10.1 디자인 원칙

- **사진·프레임이 주인공.** UI 요소는 채도·존재감을 낮춰 사진을 방해하지 않는다.
- **미니멀·화이트.** 흰 배경과 �acked 여백 중심. 장식·그라데이션·그림자 최소화.
- **터치 우선.** 행사장 태블릿과 개인 폰이 주 사용처. 마우스 호버에 의존하는 UI 금지.
- **하나의 큰 작업 영역.** 화면을 잘게 나누지 않는다. 미리보기가 항상 가장 크다.

### 10.2 색상

| 용도 | 값 | 비고 |
|---|---|---|
| 배경 (앱 전체) | `#FFFFFF` | 순백 |
| 배경 (보조 패널) | `#F5F5F5` | 살짝 회색, 영역 구분용 |
| 테두리·구분선 | `#E5E5E5` | 아주 옅게 |
| 기본 텍스트 | `#1A1A1A` | 거의 검정 |
| 보조 텍스트 | `#8A8A8A` | 라벨·설명용 회색 |
| 강조(액션) | `#1A1A1A` | 주요 버튼은 검정 배경 + 흰 글자 |
| 강조(선택 상태) | `#1A1A1A` 테두리 | 선택된 프레임·필터 표시 |

- 컬러 포인트는 쓰지 않는다. 흑·백·회색만으로 구성해 사진 색이 튀어 보이게 한다.
- 다크 모드는 1단계 범위 밖.

### 10.3 타이포그래피

- 폰트: 시스템 산세리프 스택
  `-apple-system, "Pretendard", "Noto Sans KR", sans-serif`
  (한글 사용 환경이므로 Pretendard 또는 Noto Sans KR 우선)
- 크기: 본문 16px, 라벨 13px, 화면 제목 20px. 그 이상 키우지 않는다.
- 굵기: regular(400)와 medium(500)만. bold 남발 금지.

### 10.4 간격·모양

- 기본 간격 단위 8px. (8 / 16 / 24 / 32)
- 모서리 둥글기: 버튼·카드 12px, 큰 패널 16px.
- 그림자: 거의 쓰지 않음. 필요 시 아주 옅게 1단계만.

### 10.5 터치 규칙 (중요)

- 모든 탭 가능한 요소는 **최소 44×44px**. 슬라이더 손잡이는 더 크게.
- 버튼 사이 간격 최소 8px (오터치 방지).
- 호버로만 나타나는 정보·메뉴 금지. 모든 동작은 탭으로 완결.
- 슬라이더는 충분히 길고 손잡이가 커야 함. 행사장에서 손가락으로 조작.
- 드래그(사진 위치 조정)와 핀치(줌)는 터치 제스처로 자연스럽게.

### 10.6 레이아웃 (섹션 7 보강)

두 환경 모두 터치 기반이므로 레이아웃을 하나로 통일한다.

- **미리보기가 항상 화면에서 가장 큰 영역.** 상단 또는 중앙 고정.
- 도구는 하단 탭으로: `사진` / `보정` / `프레임` / `저장` 4개.
- 한 번에 한 탭의 도구만 보인다. 화면을 좌·우 패널로 잘게 쪼개지 않는다.
- 태블릿 가로 모드: 미리보기 좌측 크게, 도구 패널 우측. 단 도구는 여전히
  4개 그룹 탭 구조를 유지(폰과 동일한 멘탈 모델).
- 폰 세로 모드: 미리보기 위, 탭 아래.

### 10.7 컴포넌트 톤 가이드

- 버튼: 주요 동작 1개만 검정 채움(흰 글자). 나머지는 테두리만 있는 흰 버튼.
- 프레임/필터 선택: 썸네일 카드 그리드. 선택된 것은 검정 테두리로 표시.
- 슬라이더: 트랙은 옅은 회색, 채워진 부분과 손잡이는 검정.
- 아이콘: 외곽선(line) 스타일, 굵기 가늘게. 채워진 아이콘 지양.
- 로딩·빈 상태: 회색 텍스트 한 줄로 담백하게.

### 10.8 1단계 디자인 완료 기준

- [ ] 전체가 흑·백·회색만으로 구성 (컬러 포인트 없음)
- [ ] 모든 탭 가능 요소 44×44px 이상
- [ ] 미리보기가 화면 최대 영역
- [ ] 하단 4탭 구조 (사진/보정/프레임/저장)
- [ ] 폰 세로·태블릿 가로 모두 동일한 4탭 멘탈 모델로 동작
- [ ] 호버 의존 UI 없음
